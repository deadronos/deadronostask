/**
 * Shared auth types that can be used in both client and server code
 */

export type { Session } from 'next-auth';
export type { AdapterUser, AdapterSession, AdapterAccount } from 'next-auth/adapters';
