# Tech Context

## Tooling
- Node.js, TypeScript, Vitest.
- Prettier and ESLint for formatting/linting.

## Key Commands
- `npm run test:convex` for Convex unit tests.
- `npm run lint` and `npm run format` for validation.
